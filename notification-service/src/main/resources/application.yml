spring:
  application:
    name: notification-service
  config:
    import: "configserver:"
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true

# Mail ayarları Config Server'dan geliyor (Mailpit: localhost:1025)
