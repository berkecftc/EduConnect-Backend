server:
  port: 8761
  address: 127.0.0.1
spring:
  application:
    name: discovery-server
  security:
    user:
      name: ${EUREKA_USER:educonnect}
      password: ${EUREKA_PASSWORD}
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false